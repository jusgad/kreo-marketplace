# IMPORTANTE: Este es un template. NO commitear valores reales.
# Usar Sealed Secrets, External Secrets Operator, o similar en producción.
apiVersion: v1
kind: Secret
metadata:
  name: kreo-secrets
  namespace: kreo-marketplace
type: Opaque
stringData:
  # Database
  POSTGRES_USER: "CHANGEME"
  POSTGRES_PASSWORD: "CHANGEME"
  DATABASE_URL: "postgresql://CHANGEME:CHANGEME@postgres-service:5432/kreo_db"

  # Redis
  REDIS_PASSWORD: "CHANGEME"
  REDIS_URL: "redis://:CHANGEME@redis-service:6379"

  # Elasticsearch
  ELASTIC_PASSWORD: "CHANGEME"
  ELASTICSEARCH_URL: "http://elastic:CHANGEME@elasticsearch-service:9200"

  # JWT
  JWT_ACCESS_SECRET: "CHANGEME_GENERATE_WITH_OPENSSL"
  JWT_REFRESH_SECRET: "CHANGEME_GENERATE_WITH_OPENSSL"
  SESSION_SECRET: "CHANGEME_GENERATE_WITH_OPENSSL"

  # Stripe
  STRIPE_SECRET_KEY: "CHANGEME"
  STRIPE_WEBHOOK_SECRET: "CHANGEME"

  # AWS
  AWS_ACCESS_KEY_ID: "CHANGEME"
  AWS_SECRET_ACCESS_KEY: "CHANGEME"

  # SendGrid
  SENDGRID_API_KEY: "CHANGEME"

  # Twilio
  TWILIO_AUTH_TOKEN: "CHANGEME"
